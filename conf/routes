# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /               controllers.Application.index

POST	/push/start		controllers.JsonPushParallel.start
POST	/pushStr/start		controllers.JsonPushParallelStr.start
POST	/pushOne/start		controllers.JsonPushOne.start
POST	/pushNoBrace/start		controllers.JsonPushParallelWithoutBrace.start

#debugging only, pointing the server to send data to itself
POST	/receivePush	controllers.ReceivePush.data

POST	/pull/init		controllers.JsonPull.init
GET     /pull/chunk		controllers.JsonPull.pullChunk

POST	/aggPull/init	controllers.AggPull.init
GET     /aggPull/day	controllers.AggPull.pullDay
GET     /aggPull/month	controllers.AggPull.pullMonth

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
